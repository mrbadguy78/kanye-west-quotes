###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: '3.1'
services:
    webserver:
        image: 'nginx:alpine'
        container_name: kanye-quotes-webserver
        working_dir: /var/www/html
        volumes:
            - '.:/var/www/html:rw'
            - './phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:rw'
        ports:
            - '80:80'
        privileged: true

    php-fpm:
        build: phpdocker/php-fpm
        container_name: kanye-quotes
        working_dir: /var/www/html
        volumes:
            - '.:/var/www/html:rw'
            - './phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/8.2/fpm/conf.d/99-overrides.ini:rw'
        ports:
            - '9000:9000'
        privileged: true
